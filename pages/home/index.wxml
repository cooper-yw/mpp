<!--pages/home/index.wxml-->
<view class="pages" style="width: 749rpx; height: 44rpx; display: block; box-sizing: border-box">
    <!-- <view wx:if="{{index === 0}}">
        <message></message>
    </view>
    <view wx:if="{{index === 1}}">
        <order></order>
    </view>
    <view wx:if="{{index === 2}}">
        <import src="../infomation/index.wxml"></import>
        <template is="info" content-class="content-class" head-class="head-class" image-round="image-round" image-class="image-round" data="{{bi:images,avatar:avatar}}" ></template>
    </view>
    <view wx:if="{{index === 3}}">
        <setting></setting>
    </view> -->
    <tabbar></tabbar>
</view>
<view>11111111222222222message home</view>